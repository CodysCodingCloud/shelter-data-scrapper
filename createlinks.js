import states from "./state_links.json" assert { type: "json" };

// const states_obj = JSON.parse(states);
console.log(states[0]);
// for (let [state, stateAbbreviation] in states_obj) {
// 	console.log(state);
// }
